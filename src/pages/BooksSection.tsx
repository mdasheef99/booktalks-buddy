/**
 * Books Section Page - Backward Compatibility Layer
 *
 * Re-exports the refactored BooksSection component
 * Maintains 100% backward compatibility with existing imports
 */

// Re-export the refactored component as default
export { default } from './BooksSection/BooksSection';
